# zigora-metrics

Reserved for v0.2. Port of [pingora-prometheus](https://github.com/cloudflare/pingora/tree/main/pingora-prometheus). Planned:

- Prometheus `/metrics` HTTP endpoint
- Atomic metric counters per service/upstream
- Admin status page (v0.2+)

Depends on `zigora-core` (Server integration).